<h1><%= @story.title %></h1>

<p><%= @story.body %></p>

<%= link_to "edit", edit_story_path(@story) %>
<%= link_to "delete", story_path(@story), method: :delete, data: {confirm: "Really delete the article?"} %><br>
<%= link_to "<< Back to Stories List", stories_path %>
